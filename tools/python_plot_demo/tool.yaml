id: "python_plot_demo"
name: "Python 绘图示例"
category: "可视化"
thumbnail: "cover.png"
version: "1.0"
description: "生成一张折线图 PNG，展示 Matplotlib 绘图流程。"
tags:
  - python
  - plot
  - demo

runtime:
  type: python
  entry: "scripts/generate_plot.py"
  args:
    - "--title"
    - "{{params.title}}"
    - "--points"
    - "{{params.points}}"
  shell: false
  workdir: "."
  extra_env: {}
  timeout: 0

env:
  strategy: uv
  interpreter: ""
  dependencies:
    - matplotlib
    - numpy
  cache_dir: ".venv"

params:
  - key: "title"
    label: "Chart Title"
    type: text
    required: false
    default: "Sensor Trend Example"
    description: "Used as the Matplotlib figure title."
  - key: "points"
    label: "Sample Points"
    type: int
    required: false
    default: 80
    min: 16
    max: 360
    description: "Controls how many points are rendered per line."
