id: "r_plot_demo"
name: "R 绘图示例"
category: "可视化"
thumbnail: "cover.png"
version: "1.0"
description: "使用 ggplot2 渲染能耗趋势 PNG，演示图片输出。"
tags:
  - r
  - plot
  - demo

runtime:
  type: r
  entry: "scripts/generate_plot.R"
  args:
    - "--title={{params.title}}"
    - "--points={{params.points}}"
  shell: false
  workdir: "."
  extra_env: {}
  timeout: 0

env:
  strategy: pak
  interpreter: ""
  dependencies:
    - ggplot2
  cache_dir: ".r-lib"

params:
  - key: "title"
    label: "Chart Title"
    type: text
    required: false
    default: "Energy Tracking Example"
  - key: "points"
    label: "Sample Points"
    type: int
    required: false
    default: 48
    min: 12
    max: 240
    description: "Number of timesteps rendered in the lines."
