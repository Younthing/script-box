id: "python_table_demo"
name: "Python 表格示例"
category: "数据"
thumbnail: "cover.png"
version: "1.0"
description: "生成合成 KPI 表格，输出 CSV 并在控制台预览。"
tags:
  - python
  - table
  - demo

runtime:
  type: python
  entry: "scripts/generate_table.py"
  args:
    - "--rows"
    - "{{params.rows}}"
    - "--title"
    - "{{params.title}}"
  shell: false
  workdir: "."
  extra_env: {}
  timeout: 0

env:
  strategy: uv
  interpreter: ""
  dependencies:
    - pandas
  cache_dir: ".venv"

params:
  - key: "rows"
    label: "Row Count"
    type: int
    required: false
    default: 6
    min: 3
    max: 32
    description: "Controls the number of generated records."
  - key: "title"
    label: "Table Title"
    type: text
    required: false
    default: "Quarterly KPI Overview"
    description: "Printed in the log output header."
